#lang plait

#| Map & Foldr/Flodl|#

(define alst '(1 2 3 4))
(define (inc x) (+ x 1))
(map inc alst) ;; maps values in lists to a function and returns them


(foldr + 0 alst) ;; starts an accumulator from 0 and folds the input in 1 + 2 + 3 + 4 -> works like map 

